apiVersion: v1
kind: Service
metadata:
  name: python-random-service
spec:
  selector:
    app: random-app
  ports:
    - protocol: TCP
      port: 80         
      targetPort: 8082 
  type: NodePort